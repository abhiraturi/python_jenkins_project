pipeline {
  agent any
  stages {
    stage('version') {
      steps {
        sh 'python --version'
      }
    }
    stage('hello') {
      steps {
        sh 'sudo nohup python3 app.py > log.txt 2>&1 &'
      }
    }
  }
}